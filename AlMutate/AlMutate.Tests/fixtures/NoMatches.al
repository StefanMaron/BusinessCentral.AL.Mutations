// This file contains no mutation targets.
// Comments mentioning Amount > 0 and Flag = true are ignored.
// Block comment: if Amount >= 10 then exit(true)

codeunit 50101 "No Matches"
{
    // A codeunit with only property-level content and no procedure bodies.
}
